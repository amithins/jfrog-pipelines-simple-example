template: true
valuesFilePath: values.yml

pipelines:
  - name: testSimple_1
    steps:
      - name: testSimple
        type: Bash
        execution:
          nodePool: k8s_test
          onExecute:
            - echo "executing step"
