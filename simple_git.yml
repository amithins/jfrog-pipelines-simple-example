pipelines:
  - name: Test123
    steps:
      - name: test
        type: Bash
        configuration:
          integrations:
            - name: git
        execution:
          onExecute:
            - echo "git token ${int_git_token}"
